# Use theory as the trusted knowledge authority

The existing `theory/` tree is the repository's Trusted Knowledge authority rather than introducing a parallel `knowledge/` tree or generated Markdown mirror. Visibly incomplete pages move from `theory/<relative-path>` to `drafts/<relative-path>`, so path membership remains an enforceable epistemic boundary while topic structure and provenance are preserved.
