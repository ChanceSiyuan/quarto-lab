Subject: Draft a proof for the cross-covariance term $\mathbf{V}(O, O^\dagger)$ in 3-design local shadowsContext:I am working on the variance analysis for estimating $\text{tr}(\sigma_{\mathbf{r}} \rho^2)$ using 3-design local shadows with $k=2$ replicas.Currently, I have defined the estimator operator as $O_{\mathbf{r}} = (\sigma_{\mathbf{r}} \otimes \mathbb{I}_n) \text{SWAP}^{\otimes n}$.However, since $O_{\mathbf{r}}$ is non-Hermitian, the single-shot shadow estimates can be complex-valued. To properly evaluate the variance of the real part of the estimator, I need to use the "Covariance Decomposition" lemma (from my notes).This requires calculating the cross-term variance operator:$$\mathbf{V}(O_{\mathbf{r}}, O_{\mathbf{r}}^\dagger)$$where $O_{\mathbf{r}}^\dagger = (\mathbb{I}_n \otimes \sigma_{\mathbf{r}}) \text{SWAP}^{\otimes n}$.Task:Please write a new section for my .qmd file (using the same style, notation, and callout blocks as 3d2c_appli1.qmd) that defines and proves this cross-covariance term.Specific Mathematical Details:Definitions:$O_{\mathbf{r}} = \sum_{\mathbf{p}} (\sigma_{\mathbf{r}}\sigma_{\mathbf{p}} \otimes \sigma_{\mathbf{p}})$.$O_{\mathbf{r}}^\dagger = \sum_{\mathbf{q}} (\sigma_{\mathbf{q}} \otimes \sigma_{\mathbf{r}}\sigma_{\mathbf{q}})$. (Please verify this Pauli decomposition derived from $(\mathbb{I} \otimes \sigma_{\mathbf{r}})\text{SWAP}$).Goal:Derive the local covariance factor $v_{cross}(\mathbf{r}_j, \mathbf{r}_j)$ for the case $\mathbf{V}(O_{\mathbf{r}}, O_{\mathbf{r}}^\dagger)$.Use the definition: $\mathbf{V}(A, B) = \sum_{W,W'} \alpha_W \beta_{W'} R(W,W') W W'$.Refer to the scaling function $R(W,W')$ from lem-LC-shadow-kcopy.Procedure:Follow the exact same "Step 1, Step 2, Step 3" structure used in the proof of lem-covar-pauli in the uploaded file.Analyze the cases for $\mathbf{r}_j \neq \mathbf{0}$ and $\mathbf{r}_j = \mathbf{0}$.Output Format:Please output a clean Markdown block containing:A Lemma block: ::: {#lem-cross-covar ...} stating the result for $\mathbf{V}(O_{\mathbf{r}}, O_{\mathbf{r}}^\dagger)$.A Proof block: ::: {.callout-note collapse="true"} ## Proof ... showing the derivation.Please simplify the final Pauli string expression as much as possible (similar to how the self-covariance term reduced to terms like $\frac{5}{3}\sigma_{\mathbf{r}}^{\otimes 2}$).