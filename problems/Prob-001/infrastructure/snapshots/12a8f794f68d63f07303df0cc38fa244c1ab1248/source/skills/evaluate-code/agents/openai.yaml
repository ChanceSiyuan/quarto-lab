interface:
  display_name: "Evaluate Code"
  short_description: "Run distance upper bound, then optional LER"
  default_prompt: "Use $evaluate-code to evaluate this code with rstim distance upper bound and optional p=1e-3 per-round LER."
