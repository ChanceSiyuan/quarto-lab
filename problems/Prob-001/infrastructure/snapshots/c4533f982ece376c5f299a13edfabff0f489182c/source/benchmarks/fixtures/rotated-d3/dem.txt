# Minimal committed detector error model for the rotated-d3 golden fixture.
# It is provenance data for preflight contract checks, not a full benchmark input.
error(0.005) D0 L0
detector(0, 0, 0) D0
logical_observable L0
