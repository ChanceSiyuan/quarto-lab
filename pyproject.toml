[project]
name = "quarto-lab-knowledge"
version = "0.1.0"
description = "Deterministic human-and-agent interfaces for the Quarto knowledge garden."
requires-python = ">=3.11,<3.12"
dependencies = [
  "markdown-it-py==4.2.0",
  "PyYAML==6.0.3",
]

[tool.uv]
package = false
